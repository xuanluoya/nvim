local options = {
  view = {
    width = 20,
    adaptive_size = true,
    signcolumn = "no",
  },
  renderer = {
    indent_width = 1,
    icons = {
      show = {
        git = false,
      }
    },
  },
}

return options
